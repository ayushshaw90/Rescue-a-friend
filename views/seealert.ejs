<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <nav id="navbar" class="relative  shadow bg-gray-800">
        <div class="container px-6 py-4 mx-auto md:flex md:justify-between md:items-center">
            <div class="flex items-center justify-between">
                <div>
                    <a class="text-2xl font-bold transition-colors duration-300 transform text-white lg:text-3xl hover:text-gray-300"
                        href="/">Brand</a>
                </div>
                <!-- Mobile menu button -->
                <div class="flex md:hidden">
                    <button id="nav-btn" type="button"
                        class="text-gray-200 hover:text-gray-400 focus:outline-none focus:text-gray-400"
                        aria-label="toggle menu">
                        <svg id="open" xmlns="http://www.w3.org/2000/svg" class="w-6 h-6" fill="none"
                            viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M4 8h16M4 16h16" />
                        </svg>

                        <svg id="close" xmlns="http://www.w3.org/2000/svg" class="w-6 h-6" fill="none"
                            viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </button>
                </div>
            </div>
            <!-- Mobile Menu open: "block", Menu closed: "hidden" -->
            <div id="hamburger" x-cloak :class="[isOpen ? 'translate-x-0 opacity-100 ' : 'opacity-0 -translate-x-full']"
                class="absolute inset-x-0 z-20 w-full px-6 py-4 transition-all duration-300 ease-in-out bg-gray-800 md:mt-0 md:p-0 md:top-0 md:relative md:bg-transparent md:w-auto md:opacity-100 md:translate-x-0 md:flex md:items-center">
                <div class="flex flex-col md:flex-row md:mx-6">
                    <a class="my-2 transition-colors duration-300 transform text-gray-200 hover:text-blue-400 md:mx-4 md:my-0"
                        href="/">Raise Alert</a>
                    <a class="my-2 transition-colors duration-300 transform text-blue-400 hover:text-blue-400 md:mx-4 md:my-0"
                        href="/see-alert">Show Alerts</a>
                    <form action="/logout?_method=DELETE" method="POST">
                        <button id="logout"
                            class="my-2 transition-colors duration-300 transform text-gray-200 hover:text-blue-400 md:mx-4 md:my-0 bg-gray-700 px-2 py rounded-lg">
                            <% if (name) { %>
                            <%=name %>
                            <% } %>
                            - Logout
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </nav>


    <div class="text-xl text-center w-full bg-gray-700" id="non-nav">
        <ul class="pt-2 px-4 max-w-md mx-auto">
            <% alerts.forEach(function(item){ %>
                <a href="/individual-alert/<%= item.email %>">
                    <li class="hover:text-blue-400 mt-4 text-gray-300 bg-gray-800 py-6 px-4 rounded">
                        <%= item.name %> has raised a panic alert!
                    </li>
                </a>
                <% }) %>
        </ul>
        <% if(isempty){ %>
            <h1 class="my-auto py-16 text-gray-200 text-2xl font-semibold">No Alerts </h1>
            <% } %>

    </div>
    <script src="/seealert_script.js"></script>
</body>

</html>